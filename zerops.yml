zerops:
  - setup: app
    build:
      base: python@3.11
      buildCommands:
        - pip install -r requirements.txt
      deployFiles:
        - ./
    run:
      base: python@3.11
      ports:
        - port: 8000
          httpSupport: true
      start: uvicorn main:app --host 0.0.0.0 --port 8000
