zerops:
  - setup: docker
    run:
      base: docker@26.1.5
      prepareCommands:
        - docker build -t job-hunter-app .
      start: docker run --network=host job-hunter-app
      ports:
        - port: 8000
          httpSupport: true
